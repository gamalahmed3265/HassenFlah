import 'package:flutter/material.dart';

class CharacterDetilesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(
        appBar: AppBar(
          title: Text('CharacterDetilesScreen'),
        ),
      ),
    );
  }
}
